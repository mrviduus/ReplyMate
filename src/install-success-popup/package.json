{
  "name": "install-success-popup",
  "version": "1.0.0",
  "description": "Auto-dismiss installation success popup component for Chrome extensions",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && npm run copy-assets",
    "copy-assets": "cp popup.html popup.css icon.png manifest.json dist/",
    "watch": "tsc --watch",
    "dev": "npm run build && npm run watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/chrome": "^0.0.246"
  }
}
